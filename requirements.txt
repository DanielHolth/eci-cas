PyYAML==6.0.3
pytest==8.4.2

# Vendor SDKs are deliberately NOT pinned here (§10.2). Every substrate
# provider imports its SDK lazily, so the ecosystem bootstraps, runs its
# deterministic tier and passes its whole offline test suite with none of
# them installed. Install only the vendor your manifest actually points
# at:
#
#   anthropic>=0.40        provider: anthropic
#   openai>=1.40           provider: openai | groq | together | mistral |
#                                    openrouter | vllm | ollama | lmstudio
#
# A substrate that is declared but whose SDK is missing fails Recovery's
# offline credential check at bootstrap (§9.1 step 6) with a message
# naming the package — it never fails mid-pipeline.
