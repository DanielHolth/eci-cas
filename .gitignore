# --- .NET ---
bin/
obj/
*.user

# Runtime output
archive.jsonl
src/EciCas.Host/telemetry/

# Secrets â€” manifest references env var NAMES only, never values.
# Real keys live in .env, which is never committed.
.env
.env.*
!.env.example

# Weights: the embedder (~90MB) and the speech model (~150MB). Downloaded by
# scripts/get-embedding-model.ps1 and scripts/get-whisper-model.ps1, never committed.
models/
__pycache__/
*.pyc
