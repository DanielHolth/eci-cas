# --- .NET ---
bin/
obj/
*.user

# Runtime output
archive.jsonl
src/EciCas.Host/telemetry/

# Secrets â€” manifest references env var NAMES only, never values.
# Real keys live in .env, which is never committed.
.env
.env.*
!.env.example

# Embedding model â€” ~90MB, downloaded not committed.
models/
__pycache__/
*.pyc
