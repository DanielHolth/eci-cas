# Development / CI extras.
#
# Vendor SDKs are not in requirements.txt on purpose (§10.2): every
# provider imports its SDK lazily, so the ecosystem bootstraps, runs its
# deterministic tier and passes its whole offline suite with none of them
# installed. You only need the vendor your manifest actually points at.
#
# These are here so tests/test_substrate_providers.py can run the REAL
# vendor SDKs against a local wire stub — no key, no network, no cost.
# Worth installing in CI: an SDK upgrade that changes the request shape
# should fail there rather than on the first live call.

-r requirements.txt

anthropic>=1.0.0
openai>=1.40.0
